# pieces-of-code
Some pieces of code that can be of use on their own or as part of larger applications.<br>
I tend to use C++ by default if using something else is not more convenient.<br>
If a program only uses syntax that's common in most programming languages, I tend to use C++.
